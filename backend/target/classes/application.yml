spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hnusteasyweibo?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 611630
    driver-class-name: com.mysql.cj.jdbc.Driver

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

file:
  upload-dir: ./uploads

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.hnusteasyweibo.entity
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

server:
  port: 8081
  servlet:
    context-path: /api

jwt:
  secret: hnusteasyweibo_secret_key_that_is_at_least_256_bits_long_for_security
  expiration: 86400000
